# Night-16E reviewer risk register

| Risk | Current evidence | Required resolution |
|---|---|---|
| The method is only a renamed Potts/CRF | Potts and alpha-expansion are explicitly attributed; only the relation modulation/self-return combination is tested | Formalize the combined objective and compare against same-start base Potts, pure support and stay-off on more studies |
| Boundary/conflict claims exceed evidence | Human `BOUNDARY_OFF` and `CONFLICT_PRIVATE_OFF` equal full | Do not headline these terms; redesign the relation carrier prospectively and repeat frozen evaluation |
| Human transfer used hidden labels | Producer inputs contain QC-only obs columns; start uses candidate-partition consensus; official `true_label` is loaded only by a separate evaluator after artifact hash | Preserve producer/evaluator split, ordered-label hash and fresh-process replay |
| Chromatin transfer is contaminated by MISAR | `C_GEO` contains P22 and MISAR historical configs | Roles were revised before human metrics: P22+MISAR discovery, human only independent transfer |
| Existing transfer lanes are described as blind | Existing strong starts are historical per-lane public-label HPO | Say “frozen operator transfer on fixed benchmark-tuned starts,” never full blind transfer |
| Support semantic is overstated | Full uses `support_mix<1`; 40–57% residual base smoothing remains depending on profile | Say “base Potts plus tri-state modulation”; keep pure-support matched control |
| Relation-specific stay is over-attributed | Support-only keeps inherited base self-return, while stay-off disables both base and relation stay | Claim only support modulation in the inherited base-stay context; add `RELATION_STAY_OFF_BASE_STAY_ON` prospectively before any relation-stay claim |
| Edge selectivity is confounded with global attenuation | Support weighting changes both edge locations and total Potts capacity | Pre-register same-start/same-base `UNIFORM_MASS_MATCHED`, deterministic `PERMUTED_SUPPORT`, and single-view support controls; until then claim only a support-weighted operator signal |
| New human score is below reported context | Full ARI 0.516 and support-only 0.545 versus MultiGATE-reported ARI 0.60 | Treat 0.60 as protocol context, not claim SOTA; perform fair baseline run later |
| MISAR K12 is a false protocol | Compute kit has K7 reference only | Label it endpoint-K12 sensitivity against K7 reference; do not count it as a K12 annotation protocol |
| MISAR stage expansion was silently substituted | Small Zenodo bundle contains GSE205055 accessions | Preserve provenance failure; do not guess E11/E13/E18 identities or annotations |
| Melanoma K2 is manufactured from notebook clusters | MultiGATE output has WNN/Louvain, while original annotation lives in SCP2176 | Align SCP2176 tumor mask and tumor-cluster field by exact IDs before any K2 ARI |
| One independent transfer is insufficient | Only human hippocampus is a new independent study | Add a second independent RNA+chromatin study before confirmed-milestone language |
